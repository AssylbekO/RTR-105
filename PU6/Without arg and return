#include <stdio.h>

void first ();
int
main ()
{
  first ();
  return 0;
}

void
first ()
{
  int num;
  printf ("Enter a positive number: ");
  scanf ("%d", &num);

  if (num % 2 == 0)
    {
      printf ("%d is even.", num);
    }
  else
    {
      printf ("%d is odd.", num);
    }


}
